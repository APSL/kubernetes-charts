questions:

  # Images
  - variable: image.repository
    label: Varnish image
    type: string
    default: "hub.apsl.net/library/varnish"
    group: Images
  - variable: image.tag
    label: Image tag
    type: string
    default: "9508-c2529592"
    group: Images

  # Main settings
  - variable: backendHost
    label: Host for the backend proxy pass
    type: string
    group: Main settings
  - variable: probes.enabled
    label: Enable liveness probe
    type: boolean
    default: "true"
    group: Main settings
